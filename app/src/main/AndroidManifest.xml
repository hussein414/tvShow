<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.googel.tvShow">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_note"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_note"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity
            android:name="com.googel.tvShow.activities.SearchActivity"
            android:exported="true" />
        <activity
            android:name="com.googel.tvShow.activities.WatchListActivity"
            android:exported="true" />
        <activity
            android:name="com.googel.tvShow.activities.TvShowDetailActivity"
            android:exported="true" />
        <activity
            android:name="com.googel.tvShow.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>